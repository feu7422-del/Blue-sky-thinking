# 桌面图标开关 · 使用说明

一键显示 / 隐藏桌面上所有图标的小工具。

---

## 一、安装

双击 **`桌面图标开关_安装包.exe`**，会弹出一个安装窗口：

1. **安装位置** —— 默认 `%LOCALAPPDATA%\Programs\桌面图标开关`，想换就点「浏览…」。
   装在你自己的目录里，**不需要管理员权限**，也不会弹 UAC。
   如果检测到你已经装过，会自动填上原来的位置（重新安装 = 覆盖更新）。
2. **三个勾选项** —— 桌面快捷方式 / 开始菜单快捷方式 / 开机自动启动，默认全勾。
3. 点「开始安装」，装完问你「现在就用一下吗？」，选「是」就直接跑起来。

装完桌面会多一个图标：

```
桌面图标开关.lnk  →  D:\...\DesktopIconSwitcher.exe
```

---

## 二、怎么切换显示 / 隐藏

有三种方式，随便用哪种：

| 方式 | 操作 |
|---|---|
| **桌面图标** | **双击**桌面上的「桌面图标开关」 |
| **右下角托盘** | 开机自启开着的话，托盘有一个小图标，**单击**它 |
| **设置窗口** | 点「立即切换显示 / 隐藏」按钮 |

> 为什么桌面那个要双击？这是 Windows 自己的规矩：桌面图标默认就是双击打开。
> 只有托盘图标才是单击。（如果你在「文件夹选项」里把桌面改成"单击打开项目"，那单击也能用。）

**切换是立刻生效的，不会重启 explorer，不会闪屏。**

---

## 三、换成你自己的图标

1. 右下角托盘图标 → **右键** → **设置…**
2. 设置窗口里有**两个方框**：
   - 左边「**图标显示时**」—— 桌面图标显示着的时候，显示这个图标
   - 右边「**图标隐藏时**」—— 桌面图标藏起来之后，显示这个图标
3. 把一张图片**拖进方框**（也可以点一下方框去选文件），程序自动转成 .ico。
4. 换好后，桌面快捷方式和托盘图标的图标都会立刻跟着变。

**图片要求：**

- 格式：PNG / JPG / BMP / GIF / ICO
- **建议 256×256 或更大的正方形**。太小放大会糊；不是正方形会自动按中心裁成正方形（会弹提示）。
- 透明背景的 PNG 效果最好。

**想恢复默认？** 点设置窗口里的「恢复默认图标」，回到程序自带的蓝色眼睛（显示）/ 带斜杠的灰眼睛（隐藏）。

---

## 四、开机自动启动

安装时勾了「开机自动启动」，开机后程序会**静悄悄地常驻右下角托盘**，不弹窗、不占屏幕。

想关掉：托盘图标右键 → **设置…** → 取消勾选「开机自动启动（常驻右下角托盘）」。

> 关掉自启不影响桌面图标的使用 —— 双击桌面的图标照样能切换，只是托盘里没那个常驻图标了。

---

## 五、绿色单文件用法（不想安装的话）

`DesktopIconSwitcher.exe` 本身就是**绿色单文件**：不依赖任何 DLL，不用安装，
拷到哪儿都能跑，设置和自定义图标都放在它自己旁边的 `icons\` 和 `settings.ini` 里。

- 把这个 exe 拷到 U 盘 / 任意文件夹，双击就跑。
- 整个文件夹拷到另一台电脑，设置和图标跟着走。
- 放在**只读目录**（比如 Program Files）时，设置会自动改存到
  `%APPDATA%\桌面图标开关`。

> 小提醒：绿色副本运行时不会去抢桌面快捷方式。
> 桌面快捷方式只有在**它指向的那个程序确实已经不存在了**的时候，才会被自动修到当前这份程序上。

---

## 六、卸载

三种方式，任选：

1. **开始菜单** → 「桌面图标开关」文件夹 → **卸载 桌面图标开关**
2. **设置** → **应用** → **已安装的应用** → 找到「桌面图标开关」→ 卸载
3. 到安装目录双击 **`Uninstall.exe`** → 选「是」

卸载会删掉：安装目录、桌面快捷方式、开始菜单的快捷方式、开机自启项、注册表登记。
**你自己换过的图标和设置会一起删掉。**

---

## 七、静默安装 / 命令行参数（给批处理用）

安装包：

```
桌面图标开关_安装包.exe --quiet [选项]
```

| 参数 | 作用 |
|---|---|
| `--quiet`（可写 `-q` / `-s`） | 不弹界面，直接装 |
| `--dir="D:\某目录"` | 指定安装位置 |
| `--nodesktop` | 不建桌面快捷方式 |
| `--nostartmenu` | 不建开始菜单快捷方式 |
| `--noautostart` | 不设开机自启 |
| `--run` | 装完立刻启动托盘 |
| `--uninstall` | 卸载（配 `--quiet` 就是静默卸载） |

退出码：`0` 成功，`1` 安装失败，`2` 安装位置被拒绝（盘根目录 / Windows 目录 / Program Files 浅层路径）。

例子 —— 静默装到 D 盘，装完直接跑：

```
桌面图标开关_安装包.exe --quiet --dir="D:\桌面图标开关" --run
```

主程序 `DesktopIconSwitcher.exe` 支持：

| 参数 | 作用 |
|---|---|
| 不带参数 | 切换一次（桌面图标就是这么用的） |
| `--tray` | 常驻托盘运行 |
| `--settings` | 只打开设置窗口 |
| `--show` | 直接设成「显示」 |
| `--hide` | 直接设成「隐藏」 |

---

## 八、常见问题

**Q：提示"没找到桌面图标窗口"？**
资源管理器（explorer.exe）没在运行。任务管理器里把它重启一下，或者注销重登。

**Q：换了图标但桌面上的图标没变？**
按 F5 刷新一下桌面。程序已经主动通知过系统刷新，偶尔还是会慢一步。

**Q：双击桌面图标没反应？**
可能装了两份（比如装完又拷了一份绿色版）。建议只留一份，把多余的删掉，
免得两份同时开机自启互相抢。

**Q：卸载时提示"部分文件被占用"？**
程序还在托盘里跑，或者被杀软锁着。重启电脑后手动删掉那个文件夹即可。

**Q：设置文件在哪儿？**
安装目录下的 `settings.ini` 和 `icons\` 文件夹。删掉 `settings.ini` 就恢复默认设置。

**Q：出错想查日志？**
程序目录下的 `错误日志.log`（出问题时才会有内容）。

---

## 九、文件清单（安装后）

```
安装目录\
├── DesktopIconSwitcher.exe    主程序（绿色单文件）
├── Uninstall.exe              卸载程序（就是安装包本身）
├── settings.ini               设置
└── icons\
    ├── show.ico               显示态图标（默认 / 你自己换的）
    └── hide.ico               隐藏态图标（默认 / 你自己换的）
```
